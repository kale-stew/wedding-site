{
  "name": "wedding-site",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "format": "prettier --write .",
    "dev": "react-scripts start",
    "build": "npm run clean && react-scripts build",
    "clean": "rm -rf build",
    "deploy": "npm run build && gh-pages -d build"
  },
  "author": "Kylie Stewart <kylie@hey.com>",
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-grid-gallery": "^0.5.5",
    "react-scripts": "^5.0.0"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "prettier": "^2.5.1"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
